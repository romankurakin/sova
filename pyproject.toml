[project]
name = "sova"
version = "1.0.0"
description = "Local document semantic search"
requires-python = ">=3.13,<3.14"
dependencies = [
    "pymupdf-layout",
    "pymupdf4llm",
    "rich",
    "snowballstemmer",
    "sqliteai-vector",
    "textual",
]

[tool.uv]
